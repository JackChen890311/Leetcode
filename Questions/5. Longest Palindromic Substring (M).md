### Tags:
- [[String]]
- [[Dynamic Programming]]
### Notes:
- 標準 DP，`dp[i][j] = (s[i] == s[j]) and (j == i + 1 or dp[i+1][j-1])`，j == i + 1 作為 base case，要注意 base case 的建立要對
- 結果 base case 只要到長度為 2 就好了，後面的都可以 DP
- 替代做法：
	- Middle Expansion
	- Mancher's Algorithm