### Tags:
- [[Array]]
### Notes:
 - 蠻好玩的，把問題簡化成兩兩 pair 的判斷，就好做了
 - 用 stack，每次都把下個東西跟最上面的範圍 merge
 - Merge 就用 `[min(aS,bS),max(aE,bE)]` 就好
---
- 第二次做卡住，最後用 binary search 先找出插入點，再把它變成 [[56. Merge Intervals (M)]] 檢查每個 interval 再 merge 起來
