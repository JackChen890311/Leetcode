### Tags:
- [[Array]]
- [[Hash Table]]
- [[Union Find]]
### Notes:
- Need to be O(N), so sorting is out
- Transform into a set first (O(N)), then only check first element (if n-1 not in set => keep finding if n+1 in set, then n++ and update result)
- 第二次做，直覺上用 union find 來做（針對每個元素 union 他的+-1），也算是有在 O(N)但還是上面方法比較簡單