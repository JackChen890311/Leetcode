### Tags:
- [[Array]]
- [[Dynamic Programming]]
### Notes:
- 原本想用類似 Kadane's Algorithm，但這樣會 TLE
- 後來看解答發現這個奇妙的 prefix & suffix 作法，根據數學來推導的：
	- 答案必定會包含到頭或尾其中一端，不可能不包含，故頭尾開始各算一次取最大（正數 -> 越乘越大、負數 -> 兩個就可以變大）
	- 遇到 0 的話等於前面全部砍掉重來
```python
class Solution:
    def maxProduct(self, nums):
        n = len(nums)
        pre = 1
        suff = 1
        maxProd = float('-inf')

        for i in range(n):
            if pre == 0:
                pre = 1
            if suff == 0:
                suff = 1

            pre *= nums[i]
            suff *= nums[n - 1 - i]

            maxProd = max(maxProd, pre, suff)

        return maxProd
```